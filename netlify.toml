
[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  condition = "Role = 'admin'" # Optional, but netlify sometimes needs a redirect rule for SPAs
